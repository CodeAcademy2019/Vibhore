const promise = require('./an_imp_rule');

// describe('Promise Error Catch',()=>{
    
//     it('This test checks if promise is rejected',()=>{
//         return expect(promise).toThrowError('OH NOES');
//     });
    
// });